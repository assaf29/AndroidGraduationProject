<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.example.newtest">

    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".ShowUserProfileActivity"></activity>
        <activity android:name=".AcceptedPostActivity" />
        <activity android:name=".JobSeekerHomeActivity" />
        <activity android:name=".ChatActivity" />
        <activity android:name=".OfferListActivity" />
        <activity android:name=".UserProfileActivity" />
        <activity android:name=".JobSeekerSearchActivity" />
        <activity android:name=".JobSeekerActivity" />
        <activity android:name=".HomeActivity" />
        <activity android:name=".LogInActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".RegisterActivity" />
        <activity android:name=".FirstPage" />
        <activity android:name=".CommentActivity" />
        <activity android:name=".ShowPostActivity" />
        <activity android:name=".PostActivity" />
        <activity android:name=".MainActivity" />
    </application>

</manifest>